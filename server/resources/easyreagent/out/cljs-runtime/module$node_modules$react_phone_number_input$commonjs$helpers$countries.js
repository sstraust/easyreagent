shadow$provide.module$node_modules$react_phone_number_input$commonjs$helpers$countries = function(global, require, module, exports) {
  function _createForOfIteratorHelperLoose(o, allowArrayLike) {
    var it = "undefined" !== typeof Symbol && o[Symbol.iterator] || o["@@iterator"];
    if (it) {
      return (it = it.call(o)).next.bind(it);
    }
    if (Array.isArray(o) || (it = _unsupportedIterableToArray(o)) || allowArrayLike && o && "number" === typeof o.length) {
      it && (o = it);
      var i = 0;
      return function() {
        return i >= o.length ? {done:!0} : {done:!1, value:o[i++]};
      };
    }
    throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
  }
  function _unsupportedIterableToArray(o, minLen) {
    if (o) {
      if ("string" === typeof o) {
        return _arrayLikeToArray(o, minLen);
      }
      var n = Object.prototype.toString.call(o).slice(8, -1);
      "Object" === n && o.constructor && (n = o.constructor.name);
      if ("Map" === n || "Set" === n) {
        return Array.from(o);
      }
      if ("Arguments" === n || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) {
        return _arrayLikeToArray(o, minLen);
      }
    }
  }
  function _arrayLikeToArray(arr, len) {
    if (null == len || len > arr.length) {
      len = arr.length;
    }
    for (var i = 0, arr2 = Array(len); i < len; i++) {
      arr2[i] = arr[i];
    }
    return arr2;
  }
  function isCountrySupportedWithError(country, metadata) {
    if ((0,_core.isSupportedCountry)(country, metadata)) {
      return !0;
    }
    console.error("Country not found: ".concat(country));
    return !1;
  }
  Object.defineProperty(exports, "__esModule", {value:!0});
  Object.defineProperty(exports, "getCountries", {enumerable:!0, get:function() {
    return _core.getCountries;
  }});
  exports.getSupportedCountries = function(countries, metadata) {
    countries && (countries = countries.filter(function(country) {
      return isCountrySupportedWithError(country, metadata);
    }), 0 === countries.length && (countries = void 0));
    return countries;
  };
  exports.getSupportedCountryOptions = function(countryOptions, metadata) {
    if (countryOptions && (countryOptions = countryOptions.filter(function(option) {
      switch(option) {
        case "\ud83c\udf10":
        case "|":
        case "...":
        case "…":
          return !0;
        default:
          return isCountrySupportedWithError(option, metadata);
      }
    }), 0 < countryOptions.length)) {
      return countryOptions;
    }
  };
  exports.isCountrySupportedWithError = isCountrySupportedWithError;
  exports.sortCountryOptions = function(options, order) {
    if (!order) {
      return options;
    }
    var optionsOnTop = [], optionsOnBottom = [], appendTo = optionsOnTop, _loop = function() {
      var element = _step.value;
      if ("|" === element) {
        appendTo.push({divider:!0});
      } else if ("..." === element || "…" === element) {
        appendTo = optionsOnBottom;
      } else {
        var countryCode = "\ud83c\udf10" === element ? void 0 : element;
        element = options.indexOf(options.filter(function(option) {
          return option.value === countryCode;
        })[0]);
        var option = options[element];
        options.splice(element, 1);
        appendTo.push(option);
      }
    };
    order = _createForOfIteratorHelperLoose(order);
    for (var _step; !(_step = order()).done;) {
      _loop();
    }
    return optionsOnTop.concat(options).concat(optionsOnBottom);
  };
  var _core = require("module$node_modules$libphonenumber_js$core$index_cjs");
};

//# sourceMappingURL=module$node_modules$react_phone_number_input$commonjs$helpers$countries.js.map
