shadow$provide.module$node_modules$libphonenumber_js$build$tools$semver_compare = function(global, require, module, exports) {
  Object.defineProperty(exports, "__esModule", {value:!0});
  exports["default"] = function(a, b) {
    a = a.split("-");
    b = b.split("-");
    for (var pa = a[0].split("."), pb = b[0].split("."), i = 0; 3 > i; i++) {
      var na = Number(pa[i]), nb = Number(pb[i]);
      if (na > nb) {
        return 1;
      }
      if (nb > na) {
        return -1;
      }
      if (!isNaN(na) && isNaN(nb)) {
        return 1;
      }
      if (isNaN(na) && !isNaN(nb)) {
        return -1;
      }
    }
    return a[1] && b[1] ? a[1] > b[1] ? 1 : a[1] < b[1] ? -1 : 0 : !a[1] && b[1] ? 1 : a[1] && !b[1] ? -1 : 0;
  };
};

//# sourceMappingURL=module$node_modules$libphonenumber_js$build$tools$semver_compare.js.map
