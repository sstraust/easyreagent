shadow$provide.module$node_modules$libphonenumber_js$build$format = function(global, require, module, exports) {
  function _typeof(obj) {
    "@babel/helpers - typeof";
    return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function(obj) {
      return typeof obj;
    } : function(obj) {
      return obj && "function" == typeof Symbol && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj;
    }, _typeof(obj);
  }
  function _getRequireWildcardCache(nodeInterop) {
    if ("function" !== typeof WeakMap) {
      return null;
    }
    var cacheBabelInterop = new WeakMap(), cacheNodeInterop = new WeakMap();
    return (_getRequireWildcardCache = function(nodeInterop) {
      return nodeInterop ? cacheNodeInterop : cacheBabelInterop;
    })(nodeInterop);
  }
  function _interopRequireDefault(obj) {
    return obj && obj.__esModule ? obj : {"default":obj};
  }
  function _createForOfIteratorHelperLoose(o, allowArrayLike) {
    var it = "undefined" !== typeof Symbol && o[Symbol.iterator] || o["@@iterator"];
    if (it) {
      return (it = it.call(o)).next.bind(it);
    }
    if (Array.isArray(o) || (it = _unsupportedIterableToArray(o)) || allowArrayLike && o && "number" === typeof o.length) {
      it && (o = it);
      var i = 0;
      return function() {
        return i >= o.length ? {done:!0} : {done:!1, value:o[i++]};
      };
    }
    throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
  }
  function _unsupportedIterableToArray(o, minLen) {
    if (o) {
      if ("string" === typeof o) {
        return _arrayLikeToArray(o, minLen);
      }
      var n = Object.prototype.toString.call(o).slice(8, -1);
      "Object" === n && o.constructor && (n = o.constructor.name);
      if ("Map" === n || "Set" === n) {
        return Array.from(o);
      }
      if ("Arguments" === n || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) {
        return _arrayLikeToArray(o, minLen);
      }
    }
  }
  function _arrayLikeToArray(arr, len) {
    if (null == len || len > arr.length) {
      len = arr.length;
    }
    for (var i = 0, arr2 = Array(len); i < len; i++) {
      arr2[i] = arr[i];
    }
    return arr2;
  }
  function ownKeys(object, enumerableOnly) {
    var keys = Object.keys(object);
    if (Object.getOwnPropertySymbols) {
      var symbols = Object.getOwnPropertySymbols(object);
      enumerableOnly && (symbols = symbols.filter(function(sym) {
        return Object.getOwnPropertyDescriptor(object, sym).enumerable;
      }));
      keys.push.apply(keys, symbols);
    }
    return keys;
  }
  function _objectSpread(target) {
    for (var i = 1; i < arguments.length; i++) {
      var source = null != arguments[i] ? arguments[i] : {};
      i % 2 ? ownKeys(Object(source), !0).forEach(function(key) {
        var value = source[key];
        key in target ? Object.defineProperty(target, key, {value, enumerable:!0, configurable:!0, writable:!0}) : target[key] = value;
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(target, Object.getOwnPropertyDescriptors(source)) : ownKeys(Object(source)).forEach(function(key) {
        Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key));
      });
    }
    return target;
  }
  function formatNationalNumber(number, carrierCode, formatAs, metadata, options) {
    var format = chooseFormatForNumber(metadata.formats(), number);
    return format ? (0,_formatNationalNumberUsingFormat["default"])(number, format, {useInternationalFormat:"INTERNATIONAL" === formatAs, withNationalPrefix:format.nationalPrefixIsOptionalWhenFormattingInNationalFormat() && options && !1 === options.nationalPrefix ? !1 : !0, carrierCode, metadata}) : number;
  }
  function chooseFormatForNumber(availableFormats, nationalNnumber) {
    availableFormats = _createForOfIteratorHelperLoose(availableFormats);
    for (var _step; !(_step = availableFormats()).done;) {
      _step = _step.value;
      if (0 < _step.leadingDigitsPatterns().length) {
        var lastLeadingDigitsPattern = _step.leadingDigitsPatterns()[_step.leadingDigitsPatterns().length - 1];
        if (0 !== nationalNnumber.search(lastLeadingDigitsPattern)) {
          continue;
        }
      }
      if ((0,_matchesEntirely["default"])(nationalNnumber, _step.pattern())) {
        return _step;
      }
    }
  }
  Object.defineProperty(exports, "__esModule", {value:!0});
  exports.chooseFormatForNumber = chooseFormatForNumber;
  exports["default"] = function(input, format, options, metadata) {
    options = options ? _objectSpread(_objectSpread({}, DEFAULT_OPTIONS), options) : DEFAULT_OPTIONS;
    metadata = new _metadata["default"](metadata);
    if (input.country && "001" !== input.country) {
      if (!metadata.hasCountry(input.country)) {
        throw Error("Unknown country: ".concat(input.country));
      }
      metadata.country(input.country);
    } else if (input.countryCallingCode) {
      metadata.selectNumberingPlan(input.countryCallingCode);
    } else {
      return input.phone || "";
    }
    var countryCallingCode = metadata.countryCallingCode(), nationalNumber = options.v2 ? input.nationalNumber : input.phone;
    switch(format) {
      case "NATIONAL":
        if (!nationalNumber) {
          return "";
        }
        countryCallingCode = nationalNumber = formatNationalNumber(nationalNumber, input.carrierCode, "NATIONAL", metadata, options);
        return (input = input.ext) ? (0,options.formatExtension)(countryCallingCode, input, metadata) : countryCallingCode;
      case "INTERNATIONAL":
        if (!nationalNumber) {
          return "+".concat(countryCallingCode);
        }
        nationalNumber = formatNationalNumber(nationalNumber, null, "INTERNATIONAL", metadata, options);
        countryCallingCode = nationalNumber = "+".concat(countryCallingCode, " ").concat(nationalNumber);
        return (input = input.ext) ? (0,options.formatExtension)(countryCallingCode, input, metadata) : countryCallingCode;
      case "E.164":
        return "+".concat(countryCallingCode).concat(nationalNumber);
      case "RFC3966":
        return (0,_RFC.formatRFC3966)({number:"+".concat(countryCallingCode).concat(nationalNumber), ext:input.ext});
      case "IDD":
        if (!options.fromCountry) {
          break;
        }
        var carrierCode = input.carrierCode, fromCountry = options.fromCountry;
        format = metadata;
        (0,_metadata.getCountryCallingCode)(fromCountry, format.metadata) === countryCallingCode ? (nationalNumber = formatNationalNumber(nationalNumber, carrierCode, "NATIONAL", format), countryCallingCode = "1" === countryCallingCode ? countryCallingCode + " " + nationalNumber : nationalNumber) : countryCallingCode = (carrierCode = (0,_getIddPrefix["default"])(fromCountry, void 0, format.metadata)) ? "".concat(carrierCode, " ").concat(countryCallingCode, " ").concat(formatNationalNumber(nationalNumber, 
        null, "INTERNATIONAL", format)) : void 0;
        return (input = input.ext) ? (0,options.formatExtension)(countryCallingCode, input, metadata) : countryCallingCode;
      default:
        throw Error('Unknown "format" argument passed to "formatNumber()": "'.concat(format, '"'));
    }
  };
  var _matchesEntirely = _interopRequireDefault(require("module$node_modules$libphonenumber_js$build$helpers$matchesEntirely")), _formatNationalNumberUsingFormat = _interopRequireDefault(require("module$node_modules$libphonenumber_js$build$helpers$formatNationalNumberUsingFormat")), _metadata = function(obj, nodeInterop) {
    if (!nodeInterop && obj && obj.__esModule) {
      return obj;
    }
    if (null === obj || "object" !== _typeof(obj) && "function" !== typeof obj) {
      return {"default":obj};
    }
    if ((nodeInterop = _getRequireWildcardCache(nodeInterop)) && nodeInterop.has(obj)) {
      return nodeInterop.get(obj);
    }
    var newObj = {}, hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor, key;
    for (key in obj) {
      if ("default" !== key && Object.prototype.hasOwnProperty.call(obj, key)) {
        var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null;
        desc && (desc.get || desc.set) ? Object.defineProperty(newObj, key, desc) : newObj[key] = obj[key];
      }
    }
    newObj["default"] = obj;
    nodeInterop && nodeInterop.set(obj, newObj);
    return newObj;
  }(require("module$node_modules$libphonenumber_js$build$metadata")), _getIddPrefix = _interopRequireDefault(require("module$node_modules$libphonenumber_js$build$helpers$getIddPrefix")), _RFC = require("module$node_modules$libphonenumber_js$build$helpers$RFC3966"), DEFAULT_OPTIONS = {formatExtension:function(formattedNumber, extension, metadata) {
    return "".concat(formattedNumber).concat(metadata.ext()).concat(extension);
  }};
};

//# sourceMappingURL=module$node_modules$libphonenumber_js$build$format.js.map
