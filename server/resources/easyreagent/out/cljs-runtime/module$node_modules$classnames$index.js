shadow$provide.module$node_modules$classnames$index = function(global, require, module, exports) {
  (function() {
    function classNames() {
      for (var classes = "", i = 0; i < arguments.length; i++) {
        var arg = arguments[i];
        arg && (classes = (arg = parseValue(arg)) ? classes ? classes + " " + arg : classes + arg : classes);
      }
      return classes;
    }
    function parseValue(arg) {
      if ("string" === typeof arg || "number" === typeof arg) {
        return arg;
      }
      if ("object" !== typeof arg) {
        return "";
      }
      if (Array.isArray(arg)) {
        return classNames.apply(null, arg);
      }
      if (arg.toString !== Object.prototype.toString && !arg.toString.toString().includes("[native code]")) {
        return arg.toString();
      }
      var classes = "", key;
      for (key in arg) {
        hasOwn.call(arg, key) && arg[key] && (classes = key ? classes ? classes + " " + key : classes + key : classes);
      }
      return classes;
    }
    var hasOwn = {}.hasOwnProperty;
    "undefined" !== typeof module && module.exports ? (classNames.default = classNames, module.exports = classNames) : "function" === typeof define && "object" === typeof define.amd && define.amd ? define("classnames", [], function() {
      return classNames;
    }) : window.classNames = classNames;
  })();
};

//# sourceMappingURL=module$node_modules$classnames$index.js.map
