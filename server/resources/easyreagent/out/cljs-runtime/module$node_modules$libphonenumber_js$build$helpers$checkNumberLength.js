shadow$provide.module$node_modules$libphonenumber_js$build$helpers$checkNumberLength = function(global, require, module, exports) {
  function checkNumberLengthForType(nationalNumber, type, metadata) {
    var type_info = metadata.type(type), possible_lengths = type_info && type_info.possibleLengths() || metadata.possibleLengths();
    if (!possible_lengths) {
      return "IS_POSSIBLE";
    }
    if ("FIXED_LINE_OR_MOBILE" === type) {
      if (!metadata.type("FIXED_LINE")) {
        return checkNumberLengthForType(nationalNumber, "MOBILE", metadata);
      }
      (type = metadata.type("MOBILE")) && (possible_lengths = (0,_mergeArrays["default"])(possible_lengths, type.possibleLengths()));
    } else if (type && !type_info) {
      return "INVALID_LENGTH";
    }
    nationalNumber = nationalNumber.length;
    type = possible_lengths[0];
    return type === nationalNumber ? "IS_POSSIBLE" : type > nationalNumber ? "TOO_SHORT" : possible_lengths[possible_lengths.length - 1] < nationalNumber ? "TOO_LONG" : 0 <= possible_lengths.indexOf(nationalNumber, 1) ? "IS_POSSIBLE" : "INVALID_LENGTH";
  }
  Object.defineProperty(exports, "__esModule", {value:!0});
  exports.checkNumberLengthForType = checkNumberLengthForType;
  exports["default"] = function(nationalNumber, metadata) {
    return checkNumberLengthForType(nationalNumber, void 0, metadata);
  };
  var _mergeArrays = function(obj) {
    return obj && obj.__esModule ? obj : {"default":obj};
  }(require("module$node_modules$libphonenumber_js$build$helpers$mergeArrays"));
};

//# sourceMappingURL=module$node_modules$libphonenumber_js$build$helpers$checkNumberLength.js.map
