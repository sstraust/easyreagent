shadow$provide.module$node_modules$libphonenumber_js$build$AsYouTypeFormatter_util = function(global, require, module, exports) {
  function _createForOfIteratorHelperLoose(o, allowArrayLike) {
    var it = "undefined" !== typeof Symbol && o[Symbol.iterator] || o["@@iterator"];
    if (it) {
      return (it = it.call(o)).next.bind(it);
    }
    if (Array.isArray(o) || (it = _unsupportedIterableToArray(o)) || allowArrayLike && o && "number" === typeof o.length) {
      it && (o = it);
      var i = 0;
      return function() {
        return i >= o.length ? {done:!0} : {done:!1, value:o[i++]};
      };
    }
    throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
  }
  function _unsupportedIterableToArray(o, minLen) {
    if (o) {
      if ("string" === typeof o) {
        return _arrayLikeToArray(o, minLen);
      }
      var n = Object.prototype.toString.call(o).slice(8, -1);
      "Object" === n && o.constructor && (n = o.constructor.name);
      if ("Map" === n || "Set" === n) {
        return Array.from(o);
      }
      if ("Arguments" === n || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) {
        return _arrayLikeToArray(o, minLen);
      }
    }
  }
  function _arrayLikeToArray(arr, len) {
    if (null == len || len > arr.length) {
      len = arr.length;
    }
    for (var i = 0, arr2 = Array(len); i < len; i++) {
      arr2[i] = arr[i];
    }
    return arr2;
  }
  function countOccurences(symbol, string) {
    var count = 0;
    string = _createForOfIteratorHelperLoose(string.split(""));
    for (var _step; !(_step = string()).done;) {
      _step.value === symbol && count++;
    }
    return count;
  }
  function stripNonPairedParens(string) {
    for (var dangling_braces = [], i = 0; i < string.length;) {
      "(" === string[i] ? dangling_braces.push(i) : ")" === string[i] && dangling_braces.pop(), i++;
    }
    i = 0;
    var cleared_string = "";
    dangling_braces.push(string.length);
    for (var _i = 0; _i < dangling_braces.length; _i++) {
      var index = dangling_braces[_i];
      cleared_string += string.slice(i, index);
      i = index + 1;
    }
    return cleared_string;
  }
  Object.defineProperty(exports, "__esModule", {value:!0});
  exports.DIGIT_PLACEHOLDER = void 0;
  exports.closeNonPairedParens = function(template, cut_before) {
    var retained_template = template.slice(0, cut_before), opening_braces = countOccurences("(", retained_template);
    retained_template = countOccurences(")", retained_template);
    for (opening_braces -= retained_template; 0 < opening_braces && cut_before < template.length;) {
      ")" === template[cut_before] && opening_braces--, cut_before++;
    }
    return template.slice(0, cut_before);
  };
  exports.countOccurences = countOccurences;
  exports.cutAndStripNonPairedParens = function(string, cutBeforeIndex) {
    ")" === string[cutBeforeIndex] && cutBeforeIndex++;
    return stripNonPairedParens(string.slice(0, cutBeforeIndex));
  };
  exports.populateTemplateWithDigits = function(template, position, digits) {
    digits = _createForOfIteratorHelperLoose(digits.split(""));
    for (var _step2; !(_step2 = digits()).done;) {
      _step2 = _step2.value;
      if (0 > template.slice(position + 1).search(DIGIT_PLACEHOLDER_MATCHER)) {
        return;
      }
      position = template.search(DIGIT_PLACEHOLDER_MATCHER);
      template = template.replace(DIGIT_PLACEHOLDER_MATCHER, _step2);
    }
    return [template, position];
  };
  exports.repeat = function(string, times) {
    if (1 > times) {
      return "";
    }
    for (var result = ""; 1 < times;) {
      times & 1 && (result += string), times >>= 1, string += string;
    }
    return result + string;
  };
  exports.stripNonPairedParens = stripNonPairedParens;
  exports.DIGIT_PLACEHOLDER = "x";
  var DIGIT_PLACEHOLDER_MATCHER = RegExp("x");
};

//# sourceMappingURL=module$node_modules$libphonenumber_js$build$AsYouTypeFormatter_util.js.map
