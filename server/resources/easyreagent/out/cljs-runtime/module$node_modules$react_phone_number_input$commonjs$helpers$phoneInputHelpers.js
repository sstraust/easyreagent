shadow$provide.module$node_modules$react_phone_number_input$commonjs$helpers$phoneInputHelpers = function(global, require, module, exports) {
  function _typeof(o) {
    "@babel/helpers - typeof";
    return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function(o) {
      return typeof o;
    } : function(o) {
      return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o;
    }, _typeof(o);
  }
  function _getRequireWildcardCache(e) {
    if ("function" != typeof WeakMap) {
      return null;
    }
    var r = new WeakMap(), t = new WeakMap();
    return (_getRequireWildcardCache = function(e) {
      return e ? t : r;
    })(e);
  }
  function generateNationalNumberDigits(phoneNumber) {
    return phoneNumber.formatNational().replace(/\D/g, "");
  }
  function e164(number, country, metadata) {
    if (number) {
      if ("+" === number[0]) {
        if ("+" === number) {
          return;
        }
        country = new _core.AsYouType(country, metadata);
        country.input(number);
        return country.getNumberValue();
      }
      if (country) {
        return number = getNationalSignificantNumberDigits(number, country, metadata), "+".concat((0,_core.getCountryCallingCode)(country, metadata)).concat(number || "");
      }
    }
  }
  function trimNumber(number, country, metadata) {
    var nationalSignificantNumberPart = getNationalSignificantNumberDigits(number, country, metadata);
    return nationalSignificantNumberPart && (nationalSignificantNumberPart = nationalSignificantNumberPart.length, metadata = new _core.Metadata(metadata), metadata.selectNumberingPlan(country), country = metadata.numberingPlan.possibleLengths()[metadata.numberingPlan.possibleLengths().length - 1], country = nationalSignificantNumberPart - country, 0 < country) ? number.slice(0, number.length - country) : number;
  }
  function getCountryForPartialE164Number(partialE164Number, _ref4) {
    var country = _ref4.country, countries = _ref4.countries, defaultCountry = _ref4.defaultCountry, latestCountrySelectedByUser = _ref4.latestCountrySelectedByUser, required = _ref4.required;
    _ref4 = _ref4.metadata;
    if ("+" === partialE164Number) {
      return country;
    }
    var derived_country = getCountryFromPossiblyIncompleteInternationalPhoneNumber(partialE164Number, _ref4);
    if (derived_country) {
      if (!countries || 0 <= countries.indexOf(derived_country)) {
        return derived_country;
      }
    } else {
      if (country) {
        if (couldNumberBelongToCountry(partialE164Number, country, _ref4)) {
          if (latestCountrySelectedByUser && couldNumberBelongToCountry(partialE164Number, latestCountrySelectedByUser, _ref4)) {
            return latestCountrySelectedByUser;
          }
          if (defaultCountry && couldNumberBelongToCountry(partialE164Number, defaultCountry, _ref4)) {
            return defaultCountry;
          }
        }
        if (!required) {
          return;
        }
      }
      return country;
    }
  }
  function convertInternationalPhoneDigitsToNational(input, country, metadata) {
    return 0 === input.indexOf((0,_getInternationalPhoneNumberPrefix["default"])(country, metadata)) ? (country = new _core.AsYouType(country, metadata), country.input(input), (input = country.getNumber()) ? input.formatNational().replace(/\D/g, "") : "") : input.replace(/\D/g, "");
  }
  function getCountryFromPossiblyIncompleteInternationalPhoneNumber(number, metadata) {
    metadata = new _core.AsYouType(null, metadata);
    metadata.input(number);
    return metadata.getCountry();
  }
  function compareStrings(a, b, locales) {
    return String.prototype.localeCompare ? a.localeCompare(b, locales) : a < b ? -1 : a > b ? 1 : 0;
  }
  function stripCountryCallingCode(number, country, metadata) {
    if (country) {
      if (country = "+" + (0,_core.getCountryCallingCode)(country, metadata), number.length < country.length) {
        if (0 === country.indexOf(number)) {
          return "";
        }
      } else {
        if (0 === number.indexOf(country)) {
          return number.slice(country.length);
        }
      }
    }
    country = 0;
    for (metadata = Object.keys(metadata.country_calling_codes); country < metadata.length; country++) {
      var country_calling_code = metadata[country];
      if (1 === number.indexOf(country_calling_code)) {
        return number.slice(1 + country_calling_code.length);
      }
    }
    return "";
  }
  function getNationalSignificantNumberDigits(number, country, metadata) {
    country = new _core.AsYouType(country, metadata);
    country.input(number);
    return (number = country.getNumber()) && number.nationalNumber;
  }
  function couldNumberBelongToCountry(number, country, metadata) {
    country = (0,_getInternationalPhoneNumberPrefix["default"])(country, metadata);
    for (metadata = 0; metadata < number.length && metadata < country.length;) {
      if (number[metadata] !== country[metadata]) {
        return !1;
      }
      metadata++;
    }
    return !0;
  }
  Object.defineProperty(exports, "__esModule", {value:!0});
  exports.compareStrings = compareStrings;
  exports.couldNumberBelongToCountry = couldNumberBelongToCountry;
  exports.e164 = e164;
  exports.generateNationalNumberDigits = generateNationalNumberDigits;
  exports.getCountryForPartialE164Number = getCountryForPartialE164Number;
  exports.getCountryFromPossiblyIncompleteInternationalPhoneNumber = getCountryFromPossiblyIncompleteInternationalPhoneNumber;
  exports.getCountrySelectOptions = function(_ref2) {
    var countries = _ref2.countries, countryNames = _ref2.countryNames, addInternationalOption = _ref2.addInternationalOption, compareStringsLocales = _ref2.compareStringsLocales, _compareStrings = _ref2.compareStrings;
    _compareStrings || (_compareStrings = compareStrings);
    _ref2 = countries.map(function(country) {
      return {value:country, label:countryNames[country] || country};
    });
    _ref2.sort(function(a, b) {
      return _compareStrings(a.label, b.label, compareStringsLocales);
    });
    addInternationalOption && _ref2.unshift({label:countryNames.ZZ});
    return _ref2;
  };
  exports.getInitialPhoneDigits = function(_ref6) {
    var value = _ref6.value, phoneNumber = _ref6.phoneNumber, defaultCountry = _ref6.defaultCountry, international = _ref6.international, useNationalFormat = _ref6.useNationalFormat;
    _ref6 = _ref6.metadata;
    return (!1 === international || useNationalFormat) && phoneNumber && phoneNumber.country ? generateNationalNumberDigits(phoneNumber) : !value && international && defaultCountry ? (0,_getInternationalPhoneNumberPrefix["default"])(defaultCountry, _ref6) : value;
  };
  exports.getNationalSignificantNumberDigits = getNationalSignificantNumberDigits;
  exports.getPhoneDigitsForNewCountry = function(phoneDigits, _ref3) {
    var prevCountry = _ref3.prevCountry, newCountry = _ref3.newCountry, metadata = _ref3.metadata;
    _ref3 = _ref3.useNationalFormat;
    if (prevCountry === newCountry) {
      return phoneDigits;
    }
    if (!phoneDigits) {
      return _ref3 ? "" : newCountry ? (0,_getInternationalPhoneNumberPrefix["default"])(newCountry, metadata) : "";
    }
    if (newCountry) {
      if ("+" === phoneDigits[0]) {
        if (_ref3) {
          return 0 === phoneDigits.indexOf("+" + (0,_core.getCountryCallingCode)(newCountry, metadata)) ? stripCountryCallingCode(phoneDigits, newCountry, metadata) : "";
        }
        if (prevCountry) {
          if (prevCountry = (0,_getInternationalPhoneNumberPrefix["default"])(newCountry, metadata), 0 !== phoneDigits.indexOf(prevCountry)) {
            return prevCountry;
          }
        } else {
          return prevCountry = (0,_getInternationalPhoneNumberPrefix["default"])(newCountry, metadata), 0 === phoneDigits.indexOf(prevCountry) ? phoneDigits : prevCountry;
        }
      }
    } else {
      if ("+" !== phoneDigits[0]) {
        return e164(phoneDigits, prevCountry, metadata) || "";
      }
    }
    return phoneDigits;
  };
  exports.getPreSelectedCountry = function(_ref) {
    var value = _ref.value, phoneNumber = _ref.phoneNumber, defaultCountry = _ref.defaultCountry, getAnyCountry = _ref.getAnyCountry, countries = _ref.countries, required = _ref.required;
    _ref = _ref.metadata;
    var country;
    phoneNumber && phoneNumber.country ? country = phoneNumber.country : !defaultCountry || value && !couldNumberBelongToCountry(value, defaultCountry, _ref) || (country = defaultCountry);
    countries && 0 > countries.indexOf(country) && (country = void 0);
    !country && required && countries && 0 < countries.length && (country = getAnyCountry());
    return country;
  };
  exports.onPhoneDigitsChange = function(phoneDigits, _ref5) {
    var prevPhoneDigits = _ref5.prevPhoneDigits, country = _ref5.country, defaultCountry = _ref5.defaultCountry, latestCountrySelectedByUser = _ref5.latestCountrySelectedByUser, countryRequired = _ref5.countryRequired, getAnyCountry = _ref5.getAnyCountry, countries = _ref5.countries, international = _ref5.international, limitMaxLength = _ref5.limitMaxLength, countryCallingCodeEditable = _ref5.countryCallingCodeEditable;
    _ref5 = _ref5.metadata;
    if (international && !1 === countryCallingCodeEditable && country && (countryCallingCodeEditable = (0,_getInternationalPhoneNumberPrefix["default"])(country, _ref5), 0 !== phoneDigits.indexOf(countryCallingCodeEditable))) {
      if (phoneDigits && "+" !== phoneDigits[0]) {
        phoneDigits = countryCallingCodeEditable + phoneDigits;
        var _value = e164(phoneDigits, country, _ref5);
      } else {
        phoneDigits = countryCallingCodeEditable;
      }
      return {phoneDigits, value:_value, country};
    }
    !1 === international && country && phoneDigits && "+" === phoneDigits[0] && (phoneDigits = convertInternationalPhoneDigitsToNational(phoneDigits, country, _ref5));
    phoneDigits && country && limitMaxLength && (phoneDigits = trimNumber(phoneDigits, country, _ref5));
    !phoneDigits || "+" === phoneDigits[0] || country && !international || (phoneDigits = "+" + phoneDigits);
    !phoneDigits && prevPhoneDigits && "+" === prevPhoneDigits[0] && (country = international ? void 0 : defaultCountry);
    "+" === phoneDigits && prevPhoneDigits && "+" === prevPhoneDigits[0] && 1 < prevPhoneDigits.length && (country = void 0);
    var value;
    phoneDigits && (value = "+" === phoneDigits[0] ? "+" === phoneDigits ? void 0 : country && 0 === (0,_getInternationalPhoneNumberPrefix["default"])(country, _ref5).indexOf(phoneDigits) ? void 0 : e164(phoneDigits, country, _ref5) : e164(phoneDigits, country, _ref5));
    value && (country = getCountryForPartialE164Number(value, {country, countries, defaultCountry, latestCountrySelectedByUser, required:!1, metadata:_ref5}), !1 === international && country && phoneDigits && "+" === phoneDigits[0] && (phoneDigits = convertInternationalPhoneDigitsToNational(phoneDigits, country, _ref5), value = e164(phoneDigits, country, _ref5)));
    !country && countryRequired && (country = defaultCountry || getAnyCountry());
    return {phoneDigits, country, value};
  };
  exports.parsePhoneNumber = function(value, metadata) {
    return (0,_core["default"])(value || "", metadata);
  };
  exports.stripCountryCallingCode = stripCountryCallingCode;
  exports.trimNumber = trimNumber;
  var _core = function(e, r) {
    if (!r && e && e.__esModule) {
      return e;
    }
    if (null === e || "object" != _typeof(e) && "function" != typeof e) {
      return {"default":e};
    }
    if ((r = _getRequireWildcardCache(r)) && r.has(e)) {
      return r.get(e);
    }
    var n = {__proto__:null}, a = Object.defineProperty && Object.getOwnPropertyDescriptor, u;
    for (u in e) {
      if ("default" !== u && {}.hasOwnProperty.call(e, u)) {
        var i = a ? Object.getOwnPropertyDescriptor(e, u) : null;
        i && (i.get || i.set) ? Object.defineProperty(n, u, i) : n[u] = e[u];
      }
    }
    return n["default"] = e, r && r.set(e, n), n;
  }(require("module$node_modules$libphonenumber_js$core$index_cjs")), _getInternationalPhoneNumberPrefix = function(obj) {
    return obj && obj.__esModule ? obj : {"default":obj};
  }(require("module$node_modules$react_phone_number_input$commonjs$helpers$getInternationalPhoneNumberPrefix"));
};

//# sourceMappingURL=module$node_modules$react_phone_number_input$commonjs$helpers$phoneInputHelpers.js.map
